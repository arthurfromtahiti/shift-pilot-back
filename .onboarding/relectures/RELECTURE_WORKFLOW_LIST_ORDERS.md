# Relecture — WORKFLOW_LIST_ORDERS.md

## Verdict global
À corriger — le flux principal et le bug métier sont bien compris, mais l'analyse sur-vend sa preuve sur un point important : elle présente le test rouge comme validation directe du bug alors que, dans l'environnement courant, `npm test` échoue d'abord pour une raison d'exécution distincte (`node --test test/` cherche un module `test` introuvable sous Node 24). Le fond métier est juste, mais la formulation actuelle mélange preuve code et preuve runtime.

## Problèmes bloquants
- L'analyse affirme que le bug `active=true` est "prouvé par le test rouge" et s'appuie sur `README.md:9` + `test/orders.test.js` comme si `npm test` validait directement ce scénario. Or l'exécution réelle `npm test` échoue avant d'atteindre ce test métier, avec `Error: Cannot find module '.../shift-pilot-back/test'` à cause du script [`package.json:7`]( /paperclip/instances/default/projects/be2f6065-a710-4a1d-8bb7-531efdbc6f23/6047261f-4409-4c8e-9290-61914f24a4c7/shift-pilot-back/package.json:7). Le producteur doit corriger cette preuve : soit rester strictement en `VÉRIFIÉ_CODE`, soit distinguer explicitement `npm test` cassé d'un lancement ciblé `node --test test/orders.test.js`, qui lui échoue bien sur l'assertion métier de [`test/orders.test.js:14`]( /paperclip/instances/default/projects/be2f6065-a710-4a1d-8bb7-531efdbc6f23/6047261f-4409-4c8e-9290-61914f24a4c7/shift-pilot-back/test/orders.test.js:14).

## Problèmes mineurs
- La confiance `high` reste défendable sur le code lui-même, mais elle devient trop large si elle englobe implicitement une validation runtime générique par `npm test`. Après correction du point ci-dessus, le producteur devrait soit requalifier la preuve en `VÉRIFIÉ_CODE`, soit préciser le périmètre exact de l'exécution utilisée.

## Points vérifiés et corrects
- Le point d'entrée et la séquence de filtrage sont exacts : `GET /orders` lit `userId` et `active`, choisit `getOrdersByUser(Number(userIdParam))` ou `listOrders()`, puis applique éventuellement `filterActiveOrders(result)` avant de renvoyer le JSON ([`src/server.js:18`]( /paperclip/instances/default/projects/be2f6065-a710-4a1d-8bb7-531efdbc6f23/6047261f-4409-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:18), [`src/server.js:19`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:19), [`src/server.js:20`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:20), [`src/server.js:22`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:22), [`src/server.js:23`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:23)).
- Le bug métier documenté est réel dans le code : `filterActiveOrders` compare `status !== "canceled"` alors que les données seed utilisent `"cancelled"`, donc les commandes annulées ne sont jamais retirées ([`src/routes/orders.js:18`]( /paperclip/instances/default/projects/be2f6065-a710-4a1d-8bb7-531efdbc6f23/6047261f-4409-4c8e-9290-61914f24a4c7/shift-pilot-back/src/routes/orders.js:18), [`src/routes/orders.js:23`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/routes/orders.js:23)).
- Le comportement silencieux sur `userId` invalide est correctement décrit : `Number(userIdParam)` peut produire `NaN`, et `order.userId === NaN` sera toujours faux dans `getOrdersByUser`, ce qui mène à `[]` sans erreur ([`src/server.js:22`]( /paperclip/instances/default/projects/be2f6065-a710-4a1d-8bb7-531efdbc6f23/6047261f-4409-4c8e-9290-61914f24a4c7/shift-pilot-back/src/server.js:22), [`src/routes/orders.js:14`]( /paperclip/instances/default/projects/be2f6065-a710-4c8e-9290-61914f24a4c7/shift-pilot-back/src/routes/orders.js:14)).
- Le test ciblé reproduit bien le bug métier quand on l'exécute directement : `node --test test/orders.test.js` échoue sur l'assertion `expected [1,3], actual [1,2,3]`, ce qui confirme le fond de l'analyse mais pas sa formulation actuelle sur `npm test`.

## Recommandations de correction
- Remplacer les formulations qui présentent le test rouge comme preuve simple et directe du workflow par une formulation précise : `VÉRIFIÉ_CODE` pour la cause du bug, et si le producteur garde une preuve d'exécution, mentionner séparément que `npm test` est cassé dans cet environnement tandis que `node --test test/orders.test.js` échoue bien sur le bug métier.
- Ajuster la justification de confiance pour ne pas laisser entendre qu'une validation runtime end-to-end a été observée via le script standard de test.
